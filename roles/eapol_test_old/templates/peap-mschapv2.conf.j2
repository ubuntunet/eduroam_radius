network={
        ssid="{{ ssid }}"
        key_mgmt=WPA-EAP
        eap=PEAP
        identity="{{ file.test_user_name }}"
        password="{{ test_user_password }}"
        phase2="autheap=MSCHAPV2"
}