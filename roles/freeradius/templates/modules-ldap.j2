  ldap {
    server = "{{ ldap_server }}"
    identity = "{{ ldap_identity }}"
    password = "{{ ldap_password }}"
    basedn = "{{ ldap_base_dn }}"
    filter = "{{ ldap_filter }}"
    start_tls = no
  }
