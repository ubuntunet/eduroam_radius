  ldap {
    server = "{{ ldap.server }}"
    identity = "{{ ldap_identity_ubuntunet }}"
    password = "{{ ldap_password_ubuntunet }}"
    basedn = "{{ ldap.base_dn }}"
    filter = "{{ ldap.filter }}"
    start_tls = no
  }
